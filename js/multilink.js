    // Фильтрация таблицы мультиссылок
    const $multilinkFilter = document.getElementById('multilink-filter');
    const choicesMultilinkFilter = new Choices($multilinkFilter, {
        searchEnabled: false,
        allowHTML: true
    });
